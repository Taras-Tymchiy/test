// @flow
import { StyleSheet } from 'react-native';
import { SPACING_S } from '../../styles/constants';

export default StyleSheet.create({
  header: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    marginBottom: SPACING_S
  }
});