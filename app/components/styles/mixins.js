import { COLOR_TEXT_PRIMARY, FONT_SIZE_M, COLOR_TEXT_SECONDARY, FONT_SIZE_S, FONT_SIZE_L } from "./constants";


export const textRegular = {
    fontWeight: 'normal',
    fontSize: FONT_SIZE_M,
    color: COLOR_TEXT_PRIMARY,
}

export const textMuted = {
    fontWeight: 'normal',
    fontSize: FONT_SIZE_M,
    color: COLOR_TEXT_SECONDARY,
}

export const textPrimary = {
    fontWeight: 'bold',
    fontSize: FONT_SIZE_M,
    color: COLOR_TEXT_PRIMARY,
}

export const textSecondary = {
    fontWeight: 'normal',
    fontSize: FONT_SIZE_S,
    color: COLOR_TEXT_SECONDARY,
}

export const textHeader = {
    fontWeight: 'bold',
    fontSize: FONT_SIZE_L,
    color: COLOR_TEXT_PRIMARY,
}
